# my_skp_project
git 사용 연습

# usage
```
    uvicorn main:app --reload
```

# 구조
my_skp_project/
│
└ main.py
└ temlates/                 ← html 위치
│    └ index.html
└ static/                   ← 정적 데이터 위치:css, js, 리소스
    └ assets
    │    └ *.jpg|png
    └ css
    │   └ *.css
    └  JS
        └ *.js